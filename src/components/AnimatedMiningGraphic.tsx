export const AnimatedMiningGraphic = ({ isMining: _isMining }: { isMining: boolean }) => {
    return (
        <div className="relative w-full max-w-[280px] aspect-square flex items-center justify-center py-4">
            {/* Placeholder — coin and orbital animations removed */}
        </div>
    );
};
